# Build output
bin/
obj/
out/

# User-specific VS/Rider/VS Code files
.vs/
.vscode/
.idea/
*.user
*.suo
*.userosscache
*.sln.docstates

# NuGet
*.nupkg
*.snupkg
.nuget/
packages/
# Rider generates these
*.DotSettings.user

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Logs
*.log
*.txt.log

# Test results
TestResults/
coverage/
*.coverage
*.coveragexml

# Publish output
publish/

# VS Code profiling artifacts
*.cpuprofile
vscode-profile-*

# Claude Code local config (per-developer)
.claude/settings.local.json

# Local secrets (vendor credentials for deploy/otel Collector)
deploy/otel/.env

# Local secrets (Postgres password for deploy/server Docker stack)
deploy/server/.env

# Per-developer AzureAdCiam tenant/client IDs for the WASM client (real values kept out of
# the public repo; dotnet user-secrets covers the equivalent Server-side values)
apps/GeoAssets.Web/wwwroot/appsettings.Local.json
