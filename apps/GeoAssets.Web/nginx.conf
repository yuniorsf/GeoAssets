server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Blazor WebAssembly client-side routing — fall back to index.html
    # for any path that doesn't match a static asset.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
