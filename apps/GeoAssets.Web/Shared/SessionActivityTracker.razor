@* Attaches JS user-activity listeners and starts the session timeout service. *@
@* Place once inside App.razor so it is active for the entire application lifetime. *@

@implements IAsyncDisposable
@inject IJSRuntime JS
@inject SessionTimeoutService SessionTimeout
@inject NavigationManager Nav
